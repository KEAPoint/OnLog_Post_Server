spring:
  datasource:
    url: jdbc:mysql://localhost:3306/onlog_blog?serverTimezone=Asia/Seoul&createDatabaseIfNotExist=true
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: create

jwt:
  secret-key: only-test-jwt-secret-key